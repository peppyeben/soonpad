// import idl from "@/utils/soonpad.json";
// import { IDL, type HelloAnchor } from "@/types/HelloAnchor";

// import { useAnchorWallet, useConnection } from "@solana/wallet-adapter-react";
// import { Program, Idl, AnchorProvider, setProvider } from "@coral-xyz/anchor";

// const { connection } = useConnection();
// const wallet = useAnchorWallet();

// const provider = new AnchorProvider(connection, wallet, {});
// setProvider(provider);

// const program = new Program(idl as Idl);
// // const program = new Program(idl);
// // const program = new Program(idl as HelloAnchor);

// // we can also explicitly mention the provider
// // const program = new Program(idl as CounterProgram, provider);
